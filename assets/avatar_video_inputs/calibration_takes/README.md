# Calibration takes (drop Gemini MP4s here)

Save exactly:

- `blonde_woman_8s.mp4`  — blonde woman 8s script take
- `male_8s.mp4`          — male 8s script take

Same 8s beat script for both (REST → SMILE → OPEN → SAY_HI → SURPRISE → ANGRY → TALK → REST).

After both files are here, tell the agent — Side B will collect each and we can train / compare for accuracy.
