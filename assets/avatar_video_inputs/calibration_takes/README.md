# Calibration takes (drop Gemini MP4s here)

Dense kit **v3** script (same for both subjects):

`REST → SMILE → OPEN → SAY_HI → TONGUE_TH → SURPRISE → ANGRY → BLINK → TALK → REST`

Save exactly:

- `blonde_woman_8s.mp4`  — blonde woman 8s dense+blink script take
- `male_8s.mp4`          — male 8s dense+blink script take

Prompt: `docs/AvatarCalibrationPrompt.md`

**Important:** takes without a deliberate full-lid **BLINK** window (5.1–5.7s) are incomplete for lid/`eyes_closed` collection. Replace before rebuilding.

After both files are here, tell the agent — Side B will collect each and we can train / compare for accuracy.
